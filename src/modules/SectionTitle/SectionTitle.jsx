const SectionTitle = ({ children }) => {
  return (
    <h1
      style={{
        fontWeight: 700,
        fontSize: '40px',
        color: '#000',
      }}
    >
      {children}
    </h1>
  );
};

export default SectionTitle;
